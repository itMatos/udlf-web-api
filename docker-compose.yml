services:
  backend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: udlf_web_api
    ports:
      - "8080:8080"

    volumes:
      - .:/app
      - /app/node_modules
      - ${HOST_UDLF_PATH}/udlf-api/Datasets:/app/Datasets
    restart: on-failure
