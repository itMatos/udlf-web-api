services:
  backend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: udlf_web_api
    ports:
      - "8080:8080"

    volumes:
      - .:/app
      - /app/node_modules
      - ${HOST_UDLF_PATH}:/app/Datasets
    restart: on-failure
